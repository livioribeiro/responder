routes:
  /:
    GET:
      code: 200
      data: '{ "name": "value" }'
      headers:
        X-Powered-By: java
    POST:
      code: 201
  /foo/(\d+?):
    GET:
      code: 200
      data: '{ "foo": ["bar", "baz"] }'

notfound:
  data: '{ "status": "not found" }'

settings:
  address: 0.0.0.0 # default 127.0.0.1
  port: 8000 # default 7000
  headers_replace: true
  headers:
    X-Powered-By: rust
